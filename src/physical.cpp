#include "physical.h"
// #include "world.h"

Physical::Physical():gravitates(1),checks_walls(1),attacks(0),moves(0){}

void Physical::update(Instance& I) const { // take control of instance
  
  
}
